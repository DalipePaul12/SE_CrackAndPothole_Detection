# Core Framework
fastapi
uvicorn

# Database & ORM
sqlalchemy
geoalchemy2
psycopg2-binary
alembic

# AI / Image Processing (Kailangan para sa image.py at ai.py)
Pillow
ultralytics

# Database & Connection
supabase

# Environment & Settings
python-dotenv
pydantic[email]
pydantic-settings
email-validator
typing-extensions
python-dateutil

# Authentication & Security
passlib[bcrypt]
python-jose[cryptography]
python-multipart
slowapi

# AI / Media Processing
opencv-python-headless
ffmpeg-python

# Background Jobs & Caching
redis
celery

# Logging & Testing
loguru
pytest
httpx

aiofiles

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9        # required for Form() / File() uploads

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0                # async PostgreSQL driver
alembic==1.13.3                # database migrations

# ── Settings ───────────────────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Auth / Security ────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT encode/decode
bcrypt==4.2.0                      # password + OTP hashing
passlib[bcrypt]==1.7.4             # CryptContext wrapper (optional - kept for compat)

# ── Rate limiting ──────────────────────────────────────────────────────────────
slowapi==0.1.9

# ── ML / AI ────────────────────────────────────────────────────────────────────
ultralytics==8.3.0             # YOLOv8/v11 inference
Pillow==10.4.0                 # image loading and preprocessing
httpx==0.27.2                  # async HTTP client for Hive API calls

# ── Email ─────────────────────────────────────────────────────────────────────
# resend==2.3.0                # Uncomment when ready to send real emails

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1           # .env file loading (pydantic-settings uses this)